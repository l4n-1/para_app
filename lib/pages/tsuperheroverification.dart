import 'package:flutter/material.dart';

class TsuperheroVerificationPage extends StatelessWidget {
  const TsuperheroVerificationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const MaterialApp(
      color: Colors.white,
      home: Scaffold(body: Center(child: Text('waw'))),
    );
  }
}
